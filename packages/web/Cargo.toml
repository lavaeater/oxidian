[package]
name = "web"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace = true, features = ["router"] }
vault = { workspace = true }
app = { workspace = true }
tracing = "0.1"
tracing-wasm = "0.2"
console_error_panic_hook = "0.1"

[features]
default = []
web = ["dioxus/web"]
